server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  mongodb:
    uri: mongodb://admin:admin@localhost:27017/admin?authSource=admin&maxPoolSize=500&socketTimeoutMS=30000&connectTimeoutMS=30000